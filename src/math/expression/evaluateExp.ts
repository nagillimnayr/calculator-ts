import { Stack } from 'immutable';

function evaluateExp(expression: string): string {
  const result = '';

  const numStack = Stack<number>();
  const operatorStack = Stack<string>();

  return result;
}

export default evaluateExp;
